<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- TU TEMA NORMAL (igual que antes) -->
    <style name="Theme.PrimeraClaseAndroid" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- TEMA DEL SPLASH -->
    <style name="Theme.PrimeraClaseAndroid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/umglogo</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.PrimeraClaseAndroid</item>
    </style>

</resources>
